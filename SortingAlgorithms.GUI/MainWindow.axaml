<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="SortingAlgorithms.GUI.MainWindow"
        Title="Сортировки для 6-классника"
        Width="800" Height="600">
    
    <StackPanel Margin="20" Spacing="10">
        <!-- Заголовок -->
        <TextBlock Text="🎯 Демонстрация алгоритмов сортировки" 
                   FontSize="20" FontWeight="Bold"/>
        
        <!-- Выбор алгоритма -->
        <TextBlock Text="Выберите алгоритм:" FontWeight="Bold"/>
        <StackPanel Orientation="Vertical" Spacing="5">
            <RadioButton x:Name="BubbleSortRadio" Content="🫧 BubbleSort - пузырьки всплывают!" IsChecked="True"/>
            <RadioButton x:Name="InsertionSortRadio" Content="🃏 InsertionSort - вставляем карты в руку!"/>
            <RadioButton x:Name="QuickSortRadio" Content="🎯 QuickSort - разделяй и властвуй!"/>
            <RadioButton x:Name="HeapSortRadio" Content="🏔️ HeapSort - строим пирамиду!"/>
        </StackPanel>
        
        <!-- Ввод массива -->
        <TextBlock Text="Введите числа через запятую:"/>
        <TextBox x:Name="ArrayTextBox" Text="5, 3, 8, 1, 2"/>
        
        <!-- Настройка скорости -->
        <TextBlock Text="Скорость анимации:"/>
        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock Text="Быстро" VerticalAlignment="Center"/>
            <Slider x:Name="SpeedSlider" Minimum="50" Maximum="1000" Value="500" Width="200"/>
            <TextBlock Text="Медленно" VerticalAlignment="Center"/>
            <TextBlock x:Name="SpeedValueText" Text="500мс" VerticalAlignment="Center"/>
        </StackPanel>
        
        <!-- Управление -->
        <StackPanel Orientation="Horizontal" Spacing="10">
            <Button x:Name="StartButton" Content="▶ Старт"/>
            <Button x:Name="ResetButton" Content="🔄 Сброс"/>
        </StackPanel>
        
        <!-- Визуализация -->
        <TextBlock Text="Визуализация:" FontWeight="Bold"/>
        <Border BorderBrush="Gray" BorderThickness="1" Height="200">
            <Canvas x:Name="VisualizationCanvas"/>
        </Border>
        
        <!-- Логи -->
        <TextBlock Text="Лог операций:" FontWeight="Bold"/>
        <TextBox x:Name="LogTextBox" Height="150" IsReadOnly="True"
                 TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Auto"/>
    </StackPanel>
</Window>